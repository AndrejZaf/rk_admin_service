server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rare_kickz
    username: postgres
    password: user
  jpa:
    generate-ddl: true
  application:
    name: rk-admin-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka